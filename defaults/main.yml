---
certificate_domains:
  - "{{ canonical_server_name }}"
  - "www.{{ canonical_server_name }}"
git_deploy_key: "{{ project_name }}-deploy"
python_binary: /usr/bin/python3
repo_path: "trawick/{{ project_name }}"
scratch_dir: "{{ project_dir }}/scratch"
virtualenv_binary: /usr/bin/virtualenv
extra_dotenv: {}
pip_version: "21.0.1"
